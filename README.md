# PyOnlineForecast
A package for online forecasting and hierarchical reconciliation in discrete time systems. The library was initially based on an adaptation of parts of the R package onlineforecast (https://onlineforecasting.org/), but now includes implementations of some forecast reconciliation methods.

Currently a pair of examples on online forecasting and reconciliation are provided in  the `scripts/` directory.